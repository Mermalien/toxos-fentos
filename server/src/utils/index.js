const validateSchema = require("./validateSchema");
const generateError = require("./generateError");
const saveImg = require("./saveImg");

module.exports = { validateSchema, generateError, saveImg };
